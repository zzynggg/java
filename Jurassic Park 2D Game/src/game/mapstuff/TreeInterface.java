package game.mapstuff;

/**
 * To retrieve the arraylist for the particular tree
 * because brachiosaur can each more than 1 fruit from a tree
 */
public interface TreeInterface {
    Tree tree = new Tree();
}
